spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/orders/**
            

server:
  port: 18080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/